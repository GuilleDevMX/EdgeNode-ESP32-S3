src/
├── api/            # Cliente API centralizado (Manejo de Tokens/401)
├── components/     # UI Atómica (Buttons, Cards, Modals, Layout)
├── context/        # Estado Global (AuthContext, TelemetryContext)
├── hooks/          # Lógica reutilizable (useAuth, useTelemetry, useWifi)
├── views/          # Páginas principales (Dashboard, Login, Wizard)
└── utils/          # Formateadores, validadores y constantes